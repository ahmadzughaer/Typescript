// test
console.log(Math.round(9.3))

let check = true;

function showDetails(name, salary) : string {
    if(check) {
        return `Hello ${name}, your salary is ${salary}`
    }
    return "nothing "
}